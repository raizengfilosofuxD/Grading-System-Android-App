<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.windows7.itonatalagalordthankyou">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="Welcome To GradeIT"
            android:theme="@style/AppTransparent">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".RegisterStudent" />
        <activity
            android:name=".Dashboard"
            android:label="Dashboard" />
        <activity android:name=".GradeEditDelete" />
        <activity android:name=".ShowStudentInfo" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".ListOfStudent" />
        <activity android:name=".Settings" />
        <activity android:name=".StudentActivty"></activity>
    </application>

</manifest>